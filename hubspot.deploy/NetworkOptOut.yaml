artifactBuildMetadata:
  repository: PaaS-Build-Deploy
  module: network-opt-out

type: procfile
team: hs-paas-build-deploy

description: Take out the rubbish

procfile:
  opt-out:
    cmd: .deploy_virtualenv/bin/opt-out
    type: ON_DEMAND

servers:
  qa:
  - mesos

contributors:
- jgoodwin@hubspot.com
