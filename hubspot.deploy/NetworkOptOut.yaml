buildName: git.hubteam.com-hubspot-PaaS-Build-Deploy-master-network-opt-out

type: procfile

description: Take out the rubbish

owners:
  - jgoodwin@hubspot.com

procfile:
  clean:
    cmd: .deploy_virtualenv/bin/opt-out
    type: ON_DEMAND

servers:
  qa:
    - mesos
